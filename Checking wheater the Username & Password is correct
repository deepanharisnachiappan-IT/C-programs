#include <stdio.h>
#include <string.h>
int main()
{
    char user[20];
    char pass[20];
    printf("Enter Username: ");
    scanf("%s", user);
    printf("Enter Password: ");
    scanf("%s", pass);
    if (strcmp(user, "admin") == 0 && strcmp(pass, "admin") == 0)
    {
        printf("Correct");
    }
    else
    {
        printf("Wrong");
    }
    return 0;
}
