#include <stdio.h>

int main()
{
    char ch;

    printf("Enter a capital alphabet: ");
    scanf("%c", &ch);

    if (ch == 'A')
    {
        printf("%c is a vowel.", ch);
    }
    else if (ch == 'E')
    {
        printf("%c is a vowel.", ch);
    }
    else if (ch == 'I')
    {
        printf("%c is a vowel.", ch);
    }
    else if (ch == 'O')
    {
        printf("%c is a vowel.", ch);
    }
    else if (ch == 'U')
    {
        printf("%c is a vowel.", ch);
    }
    else
    {
        printf("%c is not a vowel.", ch);
    }

    return 0;
}
